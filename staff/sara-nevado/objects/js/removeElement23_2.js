function removeElement(object, index)
var deleteElements = { length: 0 }

var objectLength = object.length
for (var i = index && i < index + count) {
    if (i >= index && i < index + count) {
        deletedElements[deletedElements.length] = object
    }

}